{
  "name": "babel-qrcode-list",
  "version": "1.0.0",
  "description": "Babel version of QR Code List client app",
  "scripts": {
    "babel-build": "./node_modules/.bin/babel ./src --out-dir ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Andrei Nicolescu",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-es2015": "^6.24.1"
  },
  "dependencies": {
    "eslint": "^5.13.0"
  }
}
